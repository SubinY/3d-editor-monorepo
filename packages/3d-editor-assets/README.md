# `@mh/3d-editor-assets`

```ts
import { panel } from '@mh/3d-editor-assets/common'
```

| 入口 | 内容 |
|------|------|
| `/common` | 通用模型（`panel`…） |
| `/crane` 等 | 有行业需求再加 |

`src/utils/` 仅包内使用（deepClone / nextId / loadImage），不对外导出。
