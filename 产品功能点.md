# 3D 编辑器产品功能点（竞品比对用）

> 基于 `@3d-editor/editor` 内核 + `apps/electrical-room` 验收 Demo。  
> Demo 仅验证内核与业务想法，**不是最终产品交互设计**。  
> 能力成熟度约 **80%**（MVP 可用，细则与行业壳未做满）。

---

## 一、产品定位

| 项 | 说明 |
|----|------|
| 形态 | **可嵌入的 2D/3D 编辑器内核 SDK**（非整包 SaaS 成品） |
| 交付物 | npm 包 `@3d-editor/editor` + peer `three` |
| 真相源 | 可序列化 `EditorDocumentJSON`（与渲染无关） |
| 不做什么 | 不绑 Vue/React；不提供壳 UI / 权限 / 落库；不含行业名词 |
| 业务扩展 | Host 注入 Catalog、约束、程序化模型、自建壳 UI |

**适用场景**：房间/车间俯视布局、柜/设备内立面排布、复合资产嵌套、只读 3D 监控预览。

---

## 二、内核能力（`@3d-editor/editor`）

### 1. 文档与数据模型

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 双拓扑文档：`scene`（场景）/ `container`（容器） | ✅ | 按编辑拓扑，不按行业 |
| 米制单位、工作区 bounds | ✅ | |
| 线段墙结构（非 polygon） | ✅ | 矩形房间 = 四段墙快捷创建 |
| 节点树 + 变换（位移/旋转/缩放） | ✅ | |
| 资产外置引用 `catalogRef(id, version)` | ✅ | 不 bake mesh |
| 业务扩展袋 `node.props` | ✅ | 内核不解释 |
| Document 命令栈 Undo / Redo | ✅ | 唯一历史源 |
| 选中模型（nodeId） | ✅ | 2D/3D 共用 |
| JSON 序列化 / 反序列化 | ✅ | Host 负责落库 |
| Schema 版本迁移 | ❌ | 仅有版本戳 |

### 2. 素材 Catalog

| 功能点 | 状态 | 备注 |
|--------|------|------|
| CatalogProvider 接口 | ✅ | 可换内存 / CDN / HTTP |
| 内存 Catalog 实现 | ✅ | |
| 素材分类：wall / fixture / equipment / component | ✅ | |
| 模型型：GLTF / 内置几何 / Host 程序化 | ✅ | procedural 由 Host resolve |
| 文档型复合资产（柜内嵌套） | ✅ | 3D 嵌套解析，深度上限 2 |
| 发布资产包 `assetPack` 快照 | ✅ | 钉死 id@version |
| PackCatalog（发布包只读加载） | ✅ | |

### 3. 2D 视口（俯视 / 立面）

| 功能点 | 状态 | 备注 |
|--------|------|------|
| Canvas 2D 投影（非 SVG） | ✅ | |
| 选择 / 平移节点 | ✅ | |
| 连续画墙（落点链 + 长度标注） | ✅ | 网格吸附 + 端点磁吸 |
| 拖墙 / 拖端点改形 | ✅ | 共享端点联动 |
| 封闭墙环自动填地板 | ✅ | 运行时，不写 schema |
| Catalog 拖放放置 | ✅ | footprint 悬浮预览 |
| 门窗柱贴墙吸附 | ✅ | fixture 语义 |
| 贴边对齐吸附 | ✅ | 会话开关；非硬网格量化 |
| AABB 碰撞检测 | ✅ | 重叠拒绝；可开关 |
| 标尺 | ✅ | |
| 框选 / 多选 | ❌ | 演进项 |
| 墙合并等高级编辑 | ❌ | 演进项 |

### 4. 3D 视口

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 包内 ThreeRuntime（不依赖旧 engine） | ✅ | |
| GLTF / 几何 / 程序化模型加载 | ✅ | |
| document 型资产嵌套展开 | ✅ | 柜壳 + 内元件 |
| Transform Gizmo（移动 / 旋转 / 缩放） | ✅ | 白名单；scale 与碰撞互斥 |
| Orbit / 正交平面图相机 | ✅ | 写入 `defaultView` |
| 世界坐标轴角标（点轴切视角） | ✅ | |
| 聚焦选中 / 适配视图 | ✅ | |
| 悬停描边 | ✅ | |
| 指针交互：click / dblclick / longpress / hover | ✅ | |
| 节点级 VisualState 高亮（色/强度） | ✅ | 支持柜内 path；无业务状态枚举 |
| 只读预览模式 | ✅ | |
| 性能 Info（物体/顶点/三角/耗时） | ✅ | |
| 环境：背景色 / HDR、灯光、阴影 | ✅ | |
| 辅助：网格、开口盒/柜门 enclosure | ✅ | |
| 地面 / 墙体外观材质（色 + 贴图） | ✅ | |
| EffectComposer 后处理特效 | ❌ | 当前 Edges 描边方案 |

### 5. 约束与交互策略

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 内建 AABB 碰撞 | ✅ | place / transform 强制 |
| 可选 ConstraintEngine 扩展点 | ✅ | Host 可注册细则 |
| 会话开关：吸附 / 碰撞 / gizmo mode | ✅ | |
| 加载时硬约束阻断 | ❌ | load 仅警告 |

### 6. SDK 集成面

| 功能点 | 状态 | 备注 |
|--------|------|------|
| `createEditor` 一站组装 | ✅ | Document + 可选 2D/3D |
| 延迟挂载画布 | ✅ | |
| 框架无关（无 Vue/React 绑定） | ✅ | |
| 双视图共享同一 Document | ✅ | |

---

## 三、Demo 业务能力（`electrical-room`，验证向）

> 下列为 Host 侧范例能力，用于证明内核可落地行业产品；**交互与信息架构不作为最终产品设计依据**。

### 1. 资产管理

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 电柜室（scene）CRUD | ✅ | 列表 / 新建 / 编辑 / 删除 |
| 电柜（container）CRUD | ✅ | |
| 电柜版本管理（semver） | ✅ | 保存发布为场景侧资产 |
| JSON API 落盘 | ✅ | `electrical-room-api` |

### 2. 编辑工作台（壳 UI）

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 左：素材面板（按 kind 分组拖放） | ✅ | |
| 中：2D / 3D / 并排视图切换 | ✅ | |
| 右：属性 / 场景环境 / 点位绑定 | ✅ | |
| 工具条：选择、画墙、撤销重做、删除、适配 | ✅ | |
| 吸附 / 碰撞 / 标尺开关 | ✅ | |
| 3D 移动 / 旋转 | ✅ | Demo 未开放缩放 |
| 工作区 / 柜体尺寸在线修改 | ✅ | |
| 图层树 | ✅ | 基础 |
| 环境面板（背景、灯、地面、墙、相机、helpers） | ✅ | |
| 权限 / 多用户协同 | ❌ | 非目标 |

### 3. 行业语义（仅 Demo）

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 门 / 窗 / 柱 fixture | ✅ | 贴墙 |
| 柜内元器件（断路器等） | ✅ | 含 procedural 断路器模型 |
| 电柜 document 型拖入场景 | ✅ | 3D 展开柜内元件 |
| 节点点位绑定 + 条件事件（Host props） | ✅ | 验证想法 |
| Mock 点位轮询 → 状态色高亮 | ✅ | `/preview`、`/home` |

### 4. 发布与运行时

| 功能点 | 状态 | 备注 |
|--------|------|------|
| 场景发布 → Document + assetPack | ✅ | |
| 编辑预览（活 Catalog） | ✅ | `/preview` |
| 监控首页（发布包静态快照） | ✅ | `/home` |
| 真实 IoT / SCADA 对接 | ❌ | Demo 为 mock |
| 大屏看板 / 报表 | ❌ | 非目标 |

---

## 四、与竞品比对时的读法

| 维度 | 本产品 | 常见竞品成品 |
|------|--------|--------------|
| 交付形态 | **内核 SDK**，Host 自建壳 | 整站编辑器 / SaaS |
| UI / UX | Demo 仅验收，不代表最终交互 | 产品级交互已打磨 |
| 行业深度 | 电柜仅为范例；内核零行业语义 | 往往深度绑定某一行业 |
| 数据合同 | 统一 `EditorDocumentJSON` | 多为私有/闭源格式 |
| 运行时 | VisualState + 只读 3D；业务状态由 Host 映射 | 常内置告警/工单等 |
| 协同 / 权限 / 云端 | 未做 | 常为卖点 |

**比对建议**：按「内核可复用能力」与「行业成品能力」分两列；勿把 Demo 壳 UI 当作产品承诺。

---

## 五、已知缺口（约 20%）

- Catalog 远程 Provider（接口已定，实现未做）
- 2D 框选 / 多选、墙体高级编辑
- Schema 版本迁移工具
- 更丰富的约束库（通道宽度、墙内放置等细则）
- 正式产品壳：权限、协同、资产中台、真实数据源
- 大规模场景性能专项（当前有基础 perf Info）

---

*文档对应源码：`packages/editor`、`apps/electrical-room`；架构细节见 `docs/architecture.md`。*
